export enum SenderType {
  USER = "USER",
  LLM = "LLM",
}

export enum Mode {
  ANALYSIS = "ANALYSIS",
  GENERATION = "GENERATION",
  DEBUG = "DEBUG",
}

export enum UserRole {
  ADMIN = "ADMIN",
  REGULAR = "REGULAR",
  GUEST = "GUEST",
}
